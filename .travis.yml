language: node_js
node_js: stable


install:
  - cd ./mark
  - npm install

script:  
  - node index.js
  - cd ..
  - git init
  - git config user.name "vczero"
  - git config user.email "wlhmyit@126.com" 
  - ls
  - git add .
  - git commit -am 'update docs; docs -> website'
  - git checkout gh-pages
  - git merge doc
  - git add .
  - curl -u "vczero":${gh_token} https://api.github.com/user
  - git commit -am 'update website'
  - git push origin gh-pages  

#after_script:
  


branches:
  only:
    - doc
  env:
    global:
    - GH_REF: github.com/vczero/dockit.git 





